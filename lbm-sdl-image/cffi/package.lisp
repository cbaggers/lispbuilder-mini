;;; -*- lisp -*-

(in-package #:cl-user)

(defpackage #:lbm-sdl-image-cffi
  (:use #:cl #:cffi)
  (:nicknames #:sdl-image-cffi)
  (:documentation "The main package of `lbm-sdl-image'.")
  (:export

   ;; No symbols are exported from the CFFI package.
))
